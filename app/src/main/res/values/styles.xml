<resources>

    <style name="MyMaterialTheme" parent="MyMaterialTheme.Base">

    </style>
    <!-- Base application theme. -->
    <style name="MyMaterialTheme.Base" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/addButton</item>
    </style>

    <style name="AddButton" parent="Widget.MaterialComponents.Button.TextButton">
        <!-- Customize your theme here. -->
        <item name="colorControlHighlight">@color/addButton</item>
    </style>

    <style name="MyMaterialTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="MyMaterialTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

</resources>
